# Clipboard
Extensions to the TClipboard class (OnUpdate / CF_HDROP (Drag'n'Drop) support)

Replacement TClipboard class that implements an OnUpdate event (called whenever the content of the clipboard changes) as well as properties to put files on the clipboard, and read dropped/Ctrl+C copied files from the clipboard (exposed as file names).
